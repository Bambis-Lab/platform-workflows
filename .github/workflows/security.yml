name: reusable-security
on:
  workflow_call:
permissions:
  contents: read
jobs:
  security:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Security foundation placeholder
        run: echo 'SECURITY_FOUNDATION=PASS'
